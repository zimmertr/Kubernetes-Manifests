apiVersion: v1
kind: Service
metadata:
    name: mysql
    namespace: {{ namespace }}
    labels:
        app: mysql
spec:
    selector:
        app: mysql
    ports:
    - name: "tcp-mysql"
      protocol: TCP
      targetPort: 3306
      port: 3306
