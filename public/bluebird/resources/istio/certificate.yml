apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bluebird
spec:
  secretName: bluebird
  commonName: bluebirdforecast.com
  dnsNames:
    - bluebirdforecast.com
    - www.bluebirdforecast.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
