apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: personal-website
  namespace: istio-system
spec:
  selector:
    istio: gateway
  servers:
    - hosts:
        - '*'
      port:
        name: 'http'
        number: 80
        protocol: HTTP
