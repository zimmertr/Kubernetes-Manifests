apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitwardenrs-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: bitwardenrs-postgres-data
