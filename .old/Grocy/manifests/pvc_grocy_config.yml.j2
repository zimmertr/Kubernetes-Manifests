apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: grocy-config
    namespace: {{ namespace }}
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi 
    storageClassName: nfs            
    volumeName: grocy-config
