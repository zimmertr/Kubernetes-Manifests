apiVersion: v1
kind: PersistentVolume
metadata:
    name: openvpn-config
spec:
    capacity:
        storage: 2Gi
    accessModes:
        - ReadWriteMany
    hostPath:
        path: /SaturnPool/Kubernetes/OpenVPN/
