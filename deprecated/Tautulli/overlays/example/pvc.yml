apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: tautulli-config
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi
    volumeName: tautulli-config
