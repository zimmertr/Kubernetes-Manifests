apiVersion: v1
kind: PersistentVolume
metadata:
    name: unifi-controller-mongodb
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 10Gi
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ mongodb_dir }}
    storageClassName: nfs
