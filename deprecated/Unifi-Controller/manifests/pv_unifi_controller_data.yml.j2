apiVersion: v1
kind: PersistentVolume
metadata:
    name: unifi-controller-data
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 10Gi
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ data_dir }}
    storageClassName: nfs
