apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-redis-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: nextcloud-redis-data
