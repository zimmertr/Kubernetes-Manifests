apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: argo-cd
  namespace: argo-system
spec:
  hosts:
    - '*'
  http:
    - match:
      - uri:
        prefix: "/argocd"
      route:
        - destination:
          host: argocd-server
