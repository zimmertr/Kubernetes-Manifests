apiVersion: v1
kind: PersistentVolume
metadata:
    name: sonarr-config
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 2Gi
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ config_dir }}
    storageClassName: nfs
