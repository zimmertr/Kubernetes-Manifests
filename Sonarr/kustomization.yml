apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    - ns.yml
    - dep.yml
    - svc.yml
    - pv_config.yml
    - pvc_config.yml
    - pv_tv_shows.yml
    - pvc_tv_shows.yml

namespace: sonarr

commonLabels:
    app: sonarr

configMapGenerator:
    - name: env-vars
      literals:
        - TZ='America/Los_Angeles'
        - PUID='1002'
        - PGID='1007'

