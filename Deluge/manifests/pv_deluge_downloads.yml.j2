apiVersion: v1
kind: PersistentVolume
metadata:
    name: deluge-downloads
    namespace: {{ deluge_namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ deluge_downloads }}
    storageClassName: nfs
