apiVersion: v1
kind: PersistentVolume
metadata:
    name: deluge-fileserver
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: saturn.sol.milkyway
        path: /SaturnPool/
    storageClassName: nfs
