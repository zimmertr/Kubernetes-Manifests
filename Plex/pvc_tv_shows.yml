apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-tv-shows
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    volumeName: plex-tv-shows
