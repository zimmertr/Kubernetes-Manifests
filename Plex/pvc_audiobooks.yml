apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-audiobooks
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    volumeName: plex-audiobooks
