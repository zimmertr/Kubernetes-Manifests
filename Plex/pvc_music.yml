apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-music
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    volumeName: plex-music
