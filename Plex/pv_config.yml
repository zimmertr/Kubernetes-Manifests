apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-config
spec:
    capacity:
        storage: 2Gi
    accessModes:
        - ReadWriteMany
    hostPath:
        path: /SaturnPool/Kubernetes/Plex/config
