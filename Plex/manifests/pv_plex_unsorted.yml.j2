apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-unsorted
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ unsorted_dir }}
    storageClassName: nfs
