apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-anime
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ anime_dir }}
    storageClassName: nfs
