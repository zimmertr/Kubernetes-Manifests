apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-transcode
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ transcode_dir }}
    storageClassName: nfs
