apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-audiobooks
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ audiobooks_dir }}
    storageClassName: nfs
