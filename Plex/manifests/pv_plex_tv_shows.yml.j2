apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-tv-shows
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ tv_shows_dir }}
    storageClassName: nfs
