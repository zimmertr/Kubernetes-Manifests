apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-config
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi
    storageClassName: nfs
    volumeName: plex-config
