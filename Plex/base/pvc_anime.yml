apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-anime
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    storageClassName: nfs
    volumeName: plex-anime
