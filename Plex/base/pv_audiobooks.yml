apiVersion: v1
kind: PersistentVolume
metadata:
    name: plex-audiobooks
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: saturn.sol.milkyway
        path: /SaturnPool/Audiobooks
    storageClassName: nfs
