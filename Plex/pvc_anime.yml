apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: plex-anime
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    volumeName: plex-anime
