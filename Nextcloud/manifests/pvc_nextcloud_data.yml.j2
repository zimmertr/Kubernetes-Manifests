apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nextcloud-data
    namespace: {{ namespace }}
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi 
    storageClassName: nfs            
    volumeName: nextcloud-data
