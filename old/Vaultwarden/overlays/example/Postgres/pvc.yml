apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: vaultwarden-postgres-data
