apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: tautulli-config
    namespace: {{ namespace }}
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi 
    storageClassName: nfs            
    volumeName: tautulli-config
