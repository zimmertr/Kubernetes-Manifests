apiVersion: v1
kind: PersistentVolume
metadata:
    name: tautulli-plex
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 2Gi
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ plex_dir }}
    storageClassName: nfs
