apiVersion: v1
kind: PersistentVolume
metadata:
    name: tautulli-plex
spec:
    capacity:
        storage: 2Gi
    accessModes:
        - ReadWriteMany
    hostPath:
        path: /SaturnPool/Kubernetes/Plex
