apiVersion: v1
kind: PersistentVolume
metadata:
    name: radarr-fileserver
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 15Ti
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ fileserver_root }}
    storageClassName: nfs
