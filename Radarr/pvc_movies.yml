apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: radarr-movies
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 15Ti
    volumeName: radarr-movies
