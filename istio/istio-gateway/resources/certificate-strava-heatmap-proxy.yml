apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: strava-heatmap-proxy
spec:
  secretName: strava-heatmap-proxy
  commonName: stravaproxy.tjzimmerman.com
  dnsNames:
    - stravaproxy.tjzimmerman.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
