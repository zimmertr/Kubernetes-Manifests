apiVersion: v1
kind: PersistentVolume
metadata:
    name: personal-website-files
spec:
    capacity:
        storage: 1Gi
    accessModes:
        - ReadOnlyMany
    hostPath:
        path: /SaturnPool/Kubernetes/Apache/Files
