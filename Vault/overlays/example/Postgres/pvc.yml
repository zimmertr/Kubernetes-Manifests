apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: vault-postgres-data
