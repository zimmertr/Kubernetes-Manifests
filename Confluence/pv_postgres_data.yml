apiVersion: v1
kind: PersistentVolume
metadata:
    name: confluence-postgres-data
spec:
    capacity:
        storage: 100Gi
    accessModes:
        - ReadWriteMany
    hostPath:
        path: /SaturnPool/Kubernetes/Confluence/postgres
