apiVersion: v1
kind: PersistentVolume
metadata:
    name: confluence-data
    namespace: {{ namespace }}
spec:
    capacity:
        storage: 10Gi
    accessModes:
        - ReadWriteMany
    nfs:
        server: {{ nfs_server }}
        path: {{ confluence_data_dir }}
    storageClassName: nfs
