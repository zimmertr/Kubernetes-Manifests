apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: gitlab-postgres-data
