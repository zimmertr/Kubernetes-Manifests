apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: proxmox-flashpool
  volumeName: tautulli-data
