apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: grafana-data
    namespace: {{ namespace }}
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi 
    storageClassName: nfs            
    volumeName: grafana-data
