apiVersion: apps/v1
kind: Deployment
metadata:
    name: postgres
spec:
    replicas: 1
    strategy:
        type: Recreate
    selector:
        matchLabels:
            app: postgres
    template:
        metadata:
            labels:
                app: postgres
        spec:
            containers:
                - name: postgres
                  # 9.4 is the highest supported build. Jira will reject any newer database.
                  image: postgres:9.4
                  envFrom:
                  - configMapRef:
                      name: jira-env-vars
                  - secretRef:
                      name: jira-postgres-password
                  ports:
                      - name: "tcp-postgres"
                        containerPort: 5432
                        protocol: TCP
                  volumeMounts:
                      - name: jira-postgres-data
                        mountPath: /var/lib/postgresql/data
                  resources:
                      limits:
                          cpu: .5
                          memory: "512Mi"
            volumes:
                - name: jira-postgres-data
                  persistentVolumeClaim:
                    claimName: jira-postgres-data
